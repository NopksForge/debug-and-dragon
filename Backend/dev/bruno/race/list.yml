info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/race/list"
  headers:
    - name: X-Reference-Id
      value: "{{randRefID}}"
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const { v4: uuidv4 } = require('uuid');

        const uuid = uuidv4();
        bru.setVar("randRefID", uuid);

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
